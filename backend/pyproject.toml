[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]  # pycodestyle, pyflakes, isort

[tool.ruff.format]

[tool.mutmut]
paths_to_mutate = ["app"]
tests_dir = ["tests/unit"]
runner = "pytest"
debug = true